# Household Finance Ops — Weekend Check-in Kickoff

**Paste this into a new Claude Code session (working dir: the Claude folder) to continue.**
Prepared 2026-08-23 for a check-in on/after 2026-08-30. Verify live state before acting; some numbers below move with real data.

---

## 1. What this is / where things live
- **Project:** Household Finance Ops Agent — a read/organize/propose control system (NO money movement) for John + Amanda. Weekly Sunday digest email is the main surface.
- **Working dir:** `C:\Users\JohnCappuccio\OneDrive - House of Cap\Claude\household-finance-ops` (repo). Project `CLAUDE.md` has the non-negotiables.
- **Data:** Dataverse `https://org29b77f3e.crm.dynamics.com` (tables prefix `hf`). Tables: hf_bill, hf_billinstance, hf_transaction, hf_account, hf_plaiditem, hf_auditlog.
- **Function App:** `func-hfin-hf7x2` (resource group `rg-household-finance`). Weekly digest timer = Sundays 14:00 UTC, `DIGEST_SEND=true`, `DIGEST_TO=john@`, **`DIGEST_CC=amanda@johnthecap.com` (Amanda is LIVE on the weekly as of 2026-08-23)**.
- **Run scripts with `py`** (not python/python3 — Store stubs). `az login` = **John@johnthecap.com**, tenant 7e8aa92f. Memory notes: `hfops-run-scripts-env`, `hfops-ondemand-ops`.
- **Git:** branch `sprint-4-digest`, remote origin = github.com/JohntheCap/household-finance-ops. Deploy functions: from `functions/` run `func azure functionapp publish func-hfin-hf7x2 --python --build remote`. `bill_seed_review.csv` is gitignored (holds real amounts).

## 2. Current financial snapshot (as of 2026-08-23)
- **Monthly envelope (headline):** August **~$4,006** (income $8,652 − Tier 1 fixed $4,645). September flips to **~$3,598** (see income change below).
- **Income now (Aug):** VA disability $1,256.90 + Amanda/Viking Vet $3,486.24 + Oregon UI $3,908.67 = **$8,651.81/mo**.
- **Income Sept onward:** VA + Amanda + **Substrate owner draw $6,500/mo** (UI ended) = **$11,243.14/mo**.
- **Tier 1 fixed:** $4,645.39/mo (16 bills). From Sept, + **$3,000/mo savings sweep** (pay-yourself-first) = $7,645.39.
- **August spending pace:** lean — every category under budget/pace (Groceries ~$1,243/$1,800, Dining ~$205/$800 through the 23rd).

## 3. What changed this session (all audited to hf_auditlog)
- **Cancellations:** Primo water, LMNT, Fabletics, Milk delivery (Alpenrose/Smith Bros); Apple Card installment marked PAID OFF (end 2026-06-30).
- **Amounts:** Comcast $94→$70; Google Fi reset to $40 baseline (tolerance 50%, latency 30) after confirming it's a checking debit (card ...8696), not a missed bill; Apple One→iCloud+Music (−$10/mo); Xbox Game Pass cancelled (−$15/mo).
- **Debt:** Harris & Harris balance recorded ~$1,500 (~July 2027 payoff at $137.50/mo).
- **Income restructure:** Added Substrate draw (starts 2026-09-01); Oregon UI now ends 2026-08-31 (confirmed but delayed, agency will back-pay the missed weeks as a lump); added a $3,000/mo pay-yourself-first savings sweep as a future-dated Tier 1 line.
- **Digest engine:** `compute_envelope` now honors Tier 1 start/end dates and uses a NET-AWARE plan-change warning (only warns on a >=15% net drop), so the UI→draw handoff doesn't read as a false "cliff." **Deployed + live-verified.**
- **Also built/shipped earlier this session:** in-month "This month so far" budget section (checking-real + card-estimated); card-classification coverage fixes; imported July + Aug Apple Card statements.
- **Housekeeping:** Amanda added to the weekly digest; OneNote reference pages made (Financial Mgmt Agent, PDX Billing, Dual Prompt Engine); private `claude-memory` GitHub repo + a Windows scheduled task auto-committing memory every 4h. All work committed + pushed (session work through `ded2416`; this kickoff file added as `20486cf`; HEAD will be one commit past that from the hash update).

## 4. TOP open item (the priority) — draw mechanism decides the model
John is determining **how the Substrate draw arrives**, and this changes the model:
- **If it lands in the monitored USAA checking** (John sweeps to savings): current model is correct — just wire matching to EXCLUDE the savings-transfer transactions from "spending" (need the transfer descriptor / destination — likely Banner Bank "travel" account, ...5444).
- **If it direct-deposits into USAA savings** (companion to checking, bypassing checking): the $6,500 should NOT count as spendable income — flip the model so **savings→checking transfers are the income line**. Also confirm whether that USAA savings account is in the Plaid sync (if not, the draw is invisible until moved to checking).
- **Double-count guard:** until the above is wired, September "spent" could over-count real savings transfers. Wire it BEFORE the first September sweep posts.

## 5. Verification checklist for the check-in
- [ ] Did the **Aug 30 Sunday digest** go out clean to John + Amanda? (envelope ~$4,006 minus spend, NO false cliff warning). Confirm Amanda received it.
- [ ] **Sept 1 flip:** run `envelope_check.py <url> 2026-09-01` — envelope should read ~$3,598 with Substrate in, UI out.
- [ ] Did the **first Substrate draw** post? Did the **Oregon UI back-pay** land (one-time lump)?
- [ ] **Google Fi August charge** — did it post to checking? (was running late; should match now that latency=30.)
- [ ] **Tune the savings sweep** ($3,000/mo) up/down if John wants (edit `SWEEP` in `add_savings_sweep.py`, re-run).
- [ ] **Debt visibility:** get Synchrony ($100/mo) and USAA Visa ($103.50/mo) balances + payoff dates (Harris ~$1,500 / ~July 2027 already known).

## 6. Operational quick-reference
- **Refresh + resend digest:** `scripts/refresh_and_resend_digest.ps1` (re-match + send). Preview only: `/api/digest?send=false`. IMPORTANT: after changing a DIGEST_* app setting, `az functionapp restart` + wait ~50s + verify the send response's `cc` before trusting it.
- **After editing hf_bill:** run `/api/match` before the digest, or "Coming up / Needs attention" stays stale.
- **Import Apple Card CSV:** Wallet → Card → Export Transactions (custom date ranges OK) → `py scripts/ingest_applecard.py <url> "<csv>"`. Budget review only advances a month once that month's card CSV is imported.
- **Income lives in** `scripts/seed_income.py` (re-run to apply). **Savings sweep** in `scripts/add_savings_sweep.py`. **Envelope-at-date check:** `scripts/envelope_check.py <url> <dates...>`. **Active-bills dump:** `scripts/budget_opportunities.py`.
- **Deploy code changes:** from `functions/`, `func azure functionapp publish func-hfin-hf7x2 --python --build remote`.
